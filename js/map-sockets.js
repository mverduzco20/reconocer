/**
 * El runtime del mapa vive en js/map/*.js (ver map.html).
 * Este archivo se conserva solo para referencias antiguas.
 */
console.warn('[reconocer] map-sockets.js ya no carga el mapa; usa los módulos en js/map/.');
